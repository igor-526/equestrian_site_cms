# Nexora Analytics

Система аналитики для мониторинга и анализа веб-проектов с функционалом парсинга страниц, кластеризации запросов и семантического анализа.


## Технологии

- **Backend**: Python 3.13, FastAPI, SQLAlchemy, Alembic, Pandas
- **Database**: PostgreSQL
- **Containerization**: Docker, Docker Compose


## 📚 Основные сущности
Основными сущностями системы являются:
- Проекты
- Страницы
- Запросы
- Кластеры
- Интеграции
- Пользователи
- Справочники


## Структура проекта
```text
src/
├── api/                # FastAPI endpoints
├── clients/            # External service clients
├── core/               # Domain logic
│   ├── entities/       # Core domain entities
│   ├── exceptions/     # Core domain exceptions
│   ├── protocols/      # Core protocols / interfaces
│   ├── schemas/        # Core schemas data transfer objects
│   └── services/       # Domain usecases
├── depends/            # Dependencies for fastapi. Services, repositories, clients initializations
├── migration/          # Alembic migrations
├── models/             # ORM models
├── repositories/       # Implementations for repositories
├── utils/              # Infrastructure helpers
├── main.py             # Application entrypoint
├── settings.py         # Configuration
└── alembic.ini         # Alembic configuration
```


## Брифинг описания сервисов

1. **APP.**
    Запускает fastapi приложение через `src/main.py`. Всё что нужно клиенту (фронтенду) сейчас тут, вывод данных и тд. Большая часть логики заключена в dashboard сервисе. Это основной сервис для формирования таблицы.
2. **Integration Job Worker.**
    ETL сервис у которого под капотом бесконечный while цикл с 2 корутинами которые вызываются каждые 30 секунд. Сам сервис стартует из `src/utils/integration_job_worker.py`. Цель сервиса - парсить из внешних источников, преобразовать данные и положить в бд.
    
    Сервис заточен на избежание состояний логических гонок и на обработку случаев с лимитированным внешним сервисом. 


## Гайд на локальный запуск и разработку

- Создайте файл `.env`. Заполните его, используя `.env.example` в качестве примера, подставив фиктивные или реальные данные по необходимости.
- Запустите `docker compose up` и смотрите логи. Должны были накатиться миграции и создаться сиды сущностей. Иногда lifespan не успевает за Базой, нужно просто перезапуститься.
- `uv sync --all-groups --locked` должен создать виртуальное окружение для вашей IDE
- После этого вы должны быть в состоянии запустить тесты - `make test`

После этого вы должны быть в состоянии работать.


## Тестирование

Используем pytest. Тесты разделены на интеграционные и модульные. 
- Интеграционные тесты требуют чтобы был поднят контейнер с базой. Тут возможно протестировать сразу всё, АПИ, имплементации репозиториев и сами сервисы.
- Модульные тестируют только логику в `src.core.services`. Использует mock-реализации репозиториев и клиентов для тестирования сервисов в изоляции.


## Правила разработки

1. **Чистой архитектуры**
   Отделяйте бизнес-логику от деталей реализации: core должен оставаться независимым, а всё инфраструктурное — лишь подключаемым слоем. Такой подход делает проект гибким и легко поддерживаемым.
   
2. **Следуйте SOLID**
   Особенно принципу инверсии зависимостей: интерфейсы живут в core, а реализации — в инфраструктуре. Пусть ваша логика не зависит от внешних слоёв, и тогда изменения технологий никогда не сломают фундамент системы. Однако и не забываем про другие буквы в SOLID.

3. **Покрытие тестами**
   Вся бизнес-логика должна быть покрыта тестами.

4. **Минимум комментариев, чёткие docstring.**
   Избегайте лишних комментариев. Краткого docstring в начале каждого класса или функции достаточно.

5. **Современные типы.**
   Используйте новые аннотации типов Python.
   Например, пишите `def func(list[str]) -> int | None:` вместо старого стиля.

6. **Обработка ошибок.**
   Не используйте просто `ValueError` или другие общие исключения. Применяйте или расширяйте исключения из `core.exceptions` модуле. Если подходящего исключения нет, решите, является ли ситуация исключительной или клиентской, и создайте специфичное исключение при необходимости.

7. **Форматирование и линтинг.**
   Регулярно запускайте форматтеры и линтеры, особенно после завершения задачи. Вот шаги, которые нужно выполнять:

   1. `make test`
   2. `make lint`
   3. `make format`
      Если нет утилиты `make`, прочитайте команды из Makefile и выполните вручную.

8. **Datetime.**
   Всё хранить в UTC пока что.

9. **Именование тестов.**
   Следуйте формату `test__service__case`. Например: `test__login__incorrect_password`.

10. **Keyword-Only аргументы.**
   При проектировании функций или методов делайте параметры только keyword-only с помощью `*` в сигнатуре. Всегда вызывайте такие функции с явным синтаксисом `param=value`.


## Инициализация сидов

При старте приложения в рамках `lifespan` выполняется проверка наличия базовых
данных. Значения из `core/seeds` сравниваются с содержимым базы и отсутствующие
записи создаются пакетно. Повторные запуски не создают дубликаты, вся процедура
выполняется в одной транзакции.

## Локальные демо-данные

Для наполнения базы реалистичными данными для разработки предусмотрен скрипт
`maintain/generate_dev_data.py`. Он создаёт демо-пользователя и три проекта с
набором страниц, кластеров, запросов и статистик за пять дней.

Выполните команду:

```bash
make populate_demo_data
```

По умолчанию будет создан пользователь `demo_owner` с паролем `demo1234`.
